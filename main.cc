#include <iostream>

#include "utilities.h"
#include "data_handling.h"

using namespace std;

int main(int argc, char* argv[]) {
    if (argc != 2) {
        cout << "usage: " << argv[0] << " [dataset_file]" << endl;
    }
}